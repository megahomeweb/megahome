import PromoManager from "@/components/admin/promo/PromoManager";

export const metadata = {
  title: "Promo kodlar — MegaHome Ulgurji",
};

export default function PromoPage() {
  return <PromoManager />;
}
